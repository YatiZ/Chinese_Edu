import { FC } from "react"

type Props = {
    duration: number,
    endValue: number
}

const CountUpNumber: FC<Props> = props => {
    const {duration, endValue} = props
  return (
    <div>
        
    </div>
  )
}

export default CountUpNumber